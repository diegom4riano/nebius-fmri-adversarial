# Copy to .env and fill in BUCKET_ID before running make commands.
# .env is gitignored — never commit it.

# Project ID (constant)
PARENT_ID=project-e00tza0vpr005zjp61embc

# Get BUCKET_ID with:
#   nebius storage bucket get-by-name \
#     --name precision-med-hcp \
#     --parent-id ${PARENT_ID} \
#     --format jsonpath='{.metadata.id}'
BUCKET_ID=<numeric-bucket-id>

S3_BUCKET=precision-med-hcp
S3_ENDPOINT=https://storage.eu-north1.nebius.cloud
